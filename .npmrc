# 这个 lockfile 会被 CI 和所有使用者消费，resolved 里必须是官方 registry 的
# 地址。开发机常年指着国内镜像，不钉死的话 npm install 会把镜像 URL 写进
# lockfile，CI 那边直接以 EALLOWREMOTE 失败（拒绝抓取 remote 类型的包）。
#
# 装得慢的话临时加 --registry=https://registry.npmmirror.com，但**不要**把
# 由此产生的 package-lock.json 变更提交上来。
registry=https://registry.npmjs.org/
